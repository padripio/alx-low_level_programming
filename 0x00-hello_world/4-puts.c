#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Description: Prints the string "Programming is like building a multilingual puzzle"
 *              followed by a newline character using the puts function.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    char *string1 = "\"Programming is like building a multilingual puzzle";

    /* Print the string using puts */
    puts(string1);
    puts("\n");

    return (0);
}

