#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Description: Prints a specific string to the standard output.
 *              Uses printf to format and display the string.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char *string1 = "with proper grammar, but the outcome is a piece of art,\n";

	/* Print the string using printf */
	printf("%s", string1);

	return (0);
}

